---
{"schema_version":"0.1.0","work_item_id":"wi_260606e43","from_context":"ditto work handoff (declared_by=main)","original_intent":"DITTO의 4가지 축 정의 — 먼저 4축 전부 실증 후 재평가","current_state":"4축 정식화+전부 실증 완료. 재평가 문서가 낸 재설계 제안 4개는 미구현 — 다음 세션 진입점.","decisions_made":["DITTO 4축=기능 목적기둥(의도/오케스트레이션/E2E/지식); Hooks/Skills/Agents/State는 기층(혼동 금지)","축3=브라우저 E2E only(웹없으면 N/A)","축4=가치있는 durable 변경만(에이전트 판단)","축1 종료=시스템 게이트+사용자 확인 2단","4축=완전 순차 1→2→3→4"],"changed_files":[],"evidence_refs":[],"failed_or_unverified":["축2 완료게이트 false-green(verifier partial→AC over-close) 미수정","축3 어설션 자동평가 부재"],"open_threads":["재설계 제안 #1 축2 false-green 수정","재설계 제안 #2 축3 어설션 평가","재설계 제안 #3 Distribution축 설계반영+ADR","재설계 제안 #4 축3 N/A 자동분기·축1 표면화·축4 트리거"],"next_first_check":"축2 false-green 재현: wi_260606e43 N3 verifier=ac-3 PARTIAL ↔ completion.json ac-3=pass","forbidden_scope_creep":[],"artifact_available":true,"created_at":"2026-06-06T07:46:13.295Z"}
---

# Handoff — DITTO 기능 4축 실증·재평가 (wi_260606e43) + 다음 세션 진입

> 기준 HEAD = `ca29168`. 이 세션 커밋 **전부 로컬(push 안 됨)**. 같은 머신이면 그대로 이어가면 됨.

## 0. 셋업
```bash
cd /Users/incognito/dev/projects/ditto
git log --oneline -5        # ca29168이 HEAD인지
bun --version               # 1.3.14
bun install && bun test     # 0 fail 기대
bun run lint && bun run adr:guard
```
- `ditto` CLI = `bun run dev <cmd>`(전역 설치는 이전 세션에 되돌려 없음).
- `bun run dev`는 `$ bun ...` 프리픽스를 먼저 찍음 → JSON 파싱은 첫 `{`부터 잘라야 함(반복 함정).
- `/tmp`로의 `> redirect`는 PreToolUse scope-out 훅이 막음 → 회피.

## 1. 직전까지 한 일

### A. 설치 스크립트 wi_2606068sy — 6/6 완료
`install.{sh,ps1}` + `install-plugin.mjs` 5단계(register/build/place/codeql/playwright) + `ditto init` + allowlist + uninstall/status. boxwood 실타겟 설치→autopilot e2e 실증 후 **발자국 전부 되돌림**(설치 잔재 0). 기록=`reports/design/ditto-install-distribution-record.md`.

### B. 기능 4축 정식화 + 4축 전부 실증·재평가 (wi_260606e43) — 본 작업
**정정**: "DITTO 4축"=**기능 목적 기둥**(① 의도파악·동기화 ② 자율실행 오케스트레이션 ③ 브라우저 E2E ④ 지식베이스), 완전 순차 1→2→3→4. Hooks/Skills/Agents/State는 4축 아닌 **기층**(혼동 금지). canonical=`.ditto/knowledge/adr/ADR-0010`, CLAUDE.md projection됨.
- 4축 **우회 없이 전부 실제 실행**: 축1 deep-interview(readiness 0.88 READY+사용자확인2차), 축2 self-host 진짜 subagent 위임(planner→implementer→verifier), 축3 `ditto e2e` Chromium 실제 launch(`.ditto/runs/e2e_axis3_demo/journey.png`), 축4 `bridge knowledge` CLAUDE.md projection.
- 재평가 문서=`reports/design/ditto-four-axis-reassessment.md`(정정판): 기능 4축 1차, 기층 §3 격하, 실증표 §2, **false-green 발견 §2-1**.

## 2. 다음 세션 진입점 — 미구현 재설계 제안 (실증으로 도출, 우선순위순)

재설계 제안은 **전부 문서로만 있고 코드 미반영**. 다음 작업 = 택해 구현:

1. **[최우선] 축2 완료게이트 false-green 수정** — verifier가 AC를 PARTIAL 판정해도 `autopilot complete`가 "노드 outcome=pass + 증거 존재 → AC pass"로 over-close. DITTO 제1원칙(증거 없는 완료 금지) 약화. **제안: verifier 산출을 per-AC verdict 배열로 받아 완료게이트가 직접 소비**(record-result 노드-단위 outcome이 per-AC partial을 흡수 못 하게). 관련: `src/core/autopilot-loop.ts`(record-result), `src/core/autopilot-complete.ts`(AC↔노드 매핑), `src/core/gates.ts`(completionGate). **재현**: wi_260606e43 N3 verifier=ac-3 PARTIAL ↔ `.ditto/work-items/wi_260606e43/completion.json` ac-3=pass.
2. 축3 어설션 자동평가 부재 — 러너가 NL 어설션을 페이지와 대조 안 해 부당 fail. 제안: 검사가능 술어(selector/text-presence) or `unverified` 분리. 관련 `src/core/e2e/playwright-runner.mjs`, `browser.ts`.
3. Distribution 횡단축 + session-rooting invariant를 설계 본문(`ditto-claude-code-harness-design.md §4.1`)에 반영 + ADR 등록(별도 사용자 합의 — reassessment §3.5).
4. 축3 N/A 자동분기(웹 UI 프로파일), 축1 게이트+사용자확인 AND 표면화, 축4 durable-change 트리거 표면 — 미구현 제안.

## 3. 상태 / 정리
- **push 대기**: 이 세션 + 이전 세션 커밋 전부 로컬.
- stray work item `wi_260606f3k`(request=None, 초반 실패 work start 중복) — `rm -rf .ditto/work-items/wi_260606f3k` 가능. wi_2606067cu(이전 docs 작업, partial)도 종료됨.
- 메모리: `project-functional-4axes`, `project-install-script-done`.

## 4. 다음 세션 첫 프롬프트
> "이 핸드오프(`.ditto/handoff/wi_260606e43.md`) 읽고, 4축 실증에서 도출된 **재설계 제안 #1(축2 완료게이트 false-green 수정 — verifier per-AC verdict 전달)**부터 구현해줘. 재현은 wi_260606e43(N3 verifier=ac-3 PARTIAL ↔ completion.json ac-3=pass). 검증은 실패 재현 후 수정 결과 확인."
