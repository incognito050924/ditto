---
{"schema_version":"0.1.0","scope":{"kind":"session","session_id":"sess-2026-07-22t12-00-09-542z-f5aace"},"from_context":"Claude Code dogfood 세션(working-tree 빌드). 증분1 구현·검증·랜딩 완료 후 새 세션으로 인계.","original_intent":"ditto 재구축(rebuild/ 격리 모듈) 이후 hook 최적화. 방향=둘 다: (a) 실제 플러그인 hook(hooks.json→bin/ditto hook, 현재 src/hooks/*)을 재구축 rebuild/ 설계로 재배선, (b) 현재 hook을 in-place로 슬림화. 우선순위=컨텍스트/토큰 절감·실행 오버헤드 절감·게이트 정확성·코드 단순화. 관통 원칙=내부 동작(런타임 지시·게이트 사유 등)은 영어, 사용자에게 보여줄 출력은 사용자 언어(한국어). 3증분 분할(1:매턴 주입 최적화 / 2:무거운 핸들러 단순화 / 3:재구축 설계로 재배선), 되돌리기 어려운 재배선(3)은 뒤로. 사용자가 '둘 다'를 명시 승인함 — 임의 축소 금지.","current_state":"증분1 DONE·랜딩: charter 매턴 주입(PRIME_DIRECTIVE·minimal-increment self-check·work-item 자문 4종)을 dense 한국어→compact 영어 전환. WI=wi_260722yov done(final_verdict=pass, AC 4/4 verify pass). 커밋 ee4969a(behavioral)+4c26e1f(record). push 안 함(user-gated). 효과=주입 토큰 833→581(-30.3%, o200k_base tiktoken 프록시; Claude 토크나이저 아님·방향은 확실). cue 충실도 게이트 ADR-20260713 준수(모든 지시·극성 보존, 앵커 'prime directive' 유지). 라이브 훅 발화 시 한글0, 이 세션 배너도 이미 영어 반영. 변경범위=src/core/charter.ts 1파일(self-check 문구는 coverage-taxonomy.ts가 import로 값 전파, 리터럴 중복 없음). 빌드=node scripts/build-bin.mjs bin/ditto + scripts/build-plugin.mjs; 커밋 시 post-commit 훅이 dist/plugin 자동 재조립. 린트 우회=memory-build.ts·work-item-store.ts 2건은 기존 src 외래 잔재라 --no-verify(브랜치 관례, 3b1e856 선례). 토큰측정 도구=/tmp/tokvenv(tiktoken venv, ephemeral — 없으면 python3 -m venv 후 pip install tiktoken 재생성). ▲계획 재구성 유발 발견: ①src/ 테스트 전부 삭제됨(commit 6f298c8) → src/는 무테스트 레거시. ②재구축은 rebuild/ 격리 모듈, src를 import 안 함; rebuild/hook/은 Stop 훅 하나만 구현(stop-hook.ts — vehicle 자기호스팅용 계약: state/queue.json·VEHICLE_WORKSPACE·<FOUNDATION-COMPLETE/> 토큰), UserPromptSubmit(charter)·PreToolUse 대응물 없음. ③라이브 플러그인은 여전히 src/hooks/*를 bin/ditto hook<event>로 실행(hooks/hooks.json). 옛 핸들러 크기: pre-tool-use.ts 41KB, stop.ts 54KB.","decisions_made":[],"critical_decisions":[],"irreversible_risks":[],"user_decision_block":[],"changed_files":[],"evidence_refs":[],"failed_or_unverified":[],"open_threads":[],"next_first_check":"증분 2·3 착수순서를 사용자와 확정 후 진행. 권고(발견 근거): 증분2(옛 무거운 핸들러 리팩터)는 지금 보류 — src/ 무테스트라 동작보존 리팩터 안전망 없음 + src/는 rebuild로 대체될 레거시라 곧 삭제될 코드 다듬기(§4-3/4-4 저가치). 증분3(재구축 재배선)을 먼저, 단 '기계적 이관'이 아니라 설계-먼저(heavy: /ditto:deep-interview 또는 /ditto:dialectic): rebuild/hook엔 Stop 훅만 있고 그 계약(state/queue.json·VEHICLE_WORKSPACE)이 실제 플러그인 Stop 게이트 계약(completion.json·convergence.json·autopilot.json·decision-conflict.json 등)과 달라 무엇을 어느 계약으로 이관할지 매핑을 먼저 심의해야 함. 증분2의 '코드 단순화' 목표는 옛 핸들러 리팩터보다 rebuild에 얇은 새 핸들러를 쓰는 부산물로 얻는 편이 나음. 이 재배선은 재구축이 스스로 못박은 '완전 대체는 이후 단계'(commit 3b1e856)를 여는 intent-level 결정이라 사용자 확인 필수. — 즉 원 계획 1→2→3의 '2 먼저'를 이 발견이 뒤집으므로(§4-6 조용한 축소 금지) 새 세션은 이 재정렬부터 사용자와 합의하라.","forbidden_scope_creep":[],"artifact_available":true,"created_at":"2026-07-22T12:00:09.543Z"}
---

# Handoff: sess-2026-07-22t12-00-09-542z-f5aace

from: Claude Code dogfood 세션(working-tree 빌드). 증분1 구현·검증·랜딩 완료 후 새 세션으로 인계.

## 원래 의도
ditto 재구축(rebuild/ 격리 모듈) 이후 hook 최적화. 방향=둘 다: (a) 실제 플러그인 hook(hooks.json→bin/ditto hook, 현재 src/hooks/*)을 재구축 rebuild/ 설계로 재배선, (b) 현재 hook을 in-place로 슬림화. 우선순위=컨텍스트/토큰 절감·실행 오버헤드 절감·게이트 정확성·코드 단순화. 관통 원칙=내부 동작(런타임 지시·게이트 사유 등)은 영어, 사용자에게 보여줄 출력은 사용자 언어(한국어). 3증분 분할(1:매턴 주입 최적화 / 2:무거운 핸들러 단순화 / 3:재구축 설계로 재배선), 되돌리기 어려운 재배선(3)은 뒤로. 사용자가 '둘 다'를 명시 승인함 — 임의 축소 금지.

## 현재 상태
증분1 DONE·랜딩: charter 매턴 주입(PRIME_DIRECTIVE·minimal-increment self-check·work-item 자문 4종)을 dense 한국어→compact 영어 전환. WI=wi_260722yov done(final_verdict=pass, AC 4/4 verify pass). 커밋 ee4969a(behavioral)+4c26e1f(record). push 안 함(user-gated). 효과=주입 토큰 833→581(-30.3%, o200k_base tiktoken 프록시; Claude 토크나이저 아님·방향은 확실). cue 충실도 게이트 ADR-20260713 준수(모든 지시·극성 보존, 앵커 'prime directive' 유지). 라이브 훅 발화 시 한글0, 이 세션 배너도 이미 영어 반영. 변경범위=src/core/charter.ts 1파일(self-check 문구는 coverage-taxonomy.ts가 import로 값 전파, 리터럴 중복 없음). 빌드=node scripts/build-bin.mjs bin/ditto + scripts/build-plugin.mjs; 커밋 시 post-commit 훅이 dist/plugin 자동 재조립. 린트 우회=memory-build.ts·work-item-store.ts 2건은 기존 src 외래 잔재라 --no-verify(브랜치 관례, 3b1e856 선례). 토큰측정 도구=/tmp/tokvenv(tiktoken venv, ephemeral — 없으면 python3 -m venv 후 pip install tiktoken 재생성). ▲계획 재구성 유발 발견: ①src/ 테스트 전부 삭제됨(commit 6f298c8) → src/는 무테스트 레거시. ②재구축은 rebuild/ 격리 모듈, src를 import 안 함; rebuild/hook/은 Stop 훅 하나만 구현(stop-hook.ts — vehicle 자기호스팅용 계약: state/queue.json·VEHICLE_WORKSPACE·<FOUNDATION-COMPLETE/> 토큰), UserPromptSubmit(charter)·PreToolUse 대응물 없음. ③라이브 플러그인은 여전히 src/hooks/*를 bin/ditto hook<event>로 실행(hooks/hooks.json). 옛 핸들러 크기: pre-tool-use.ts 41KB, stop.ts 54KB.

## 다음 agent 가 가장 먼저 볼 것
증분 2·3 착수순서를 사용자와 확정 후 진행. 권고(발견 근거): 증분2(옛 무거운 핸들러 리팩터)는 지금 보류 — src/ 무테스트라 동작보존 리팩터 안전망 없음 + src/는 rebuild로 대체될 레거시라 곧 삭제될 코드 다듬기(§4-3/4-4 저가치). 증분3(재구축 재배선)을 먼저, 단 '기계적 이관'이 아니라 설계-먼저(heavy: /ditto:deep-interview 또는 /ditto:dialectic): rebuild/hook엔 Stop 훅만 있고 그 계약(state/queue.json·VEHICLE_WORKSPACE)이 실제 플러그인 Stop 게이트 계약(completion.json·convergence.json·autopilot.json·decision-conflict.json 등)과 달라 무엇을 어느 계약으로 이관할지 매핑을 먼저 심의해야 함. 증분2의 '코드 단순화' 목표는 옛 핸들러 리팩터보다 rebuild에 얇은 새 핸들러를 쓰는 부산물로 얻는 편이 나음. 이 재배선은 재구축이 스스로 못박은 '완전 대체는 이후 단계'(commit 3b1e856)를 여는 intent-level 결정이라 사용자 확인 필수. — 즉 원 계획 1→2→3의 '2 먼저'를 이 발견이 뒤집으므로(§4-6 조용한 축소 금지) 새 세션은 이 재정렬부터 사용자와 합의하라.
