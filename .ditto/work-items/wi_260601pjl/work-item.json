{
  "schema_version": "0.1.0",
  "id": "wi_260601pjl",
  "title": "PreToolUse safety hook (block-by-default: destructive/secret/scope-out)",
  "source_request": "PreToolUse safety work item을 다음으로 열어줘 — 정책: block-by-default, 범위는 destructive/secret/scope-out만(package provenance 제외)",
  "goal": "DITTO가 PreToolUse 훅으로 고확신 안전 위반(파괴적 명령·비밀 파일 접근·repo scope 밖 쓰기)을 기본 차단해 작업공간을 보호한다 — block-by-default, DITTO_SKIP_HOOKS escape, 기존 fail-open 런타임 정합 (post-v0 safety)",
  "acceptance_criteria": [
    {
      "id": "ac-1",
      "statement": "src/hooks/pre-tool-use.ts 핸들러가 HookHandler 인터페이스에 정합하고, 자체 에러는 fail-open(exit 0)이며, DITTO_SKIP_HOOKS가 설정되면 검사 없이 exit 0으로 통과한다.",
      "verdict": "pass",
      "evidence": [
        { "kind": "file", "path": "src/hooks/pre-tool-use.ts", "summary": "block 로직만; fail-open/kill-switch는 runHook wrapper(runtime.ts:35-45) 위임, default-allow" },
        { "kind": "command", "command": "bun test tests/hooks/pre-tool-use.test.ts", "summary": "runHook으로 SKIP_HOOKS→0·throwing-stub→0 검증(검증자 재현)" }
      ]
    },
    {
      "id": "ac-2",
      "statement": "파괴적 명령(repo/home 밖 rm -rf, 기본 브랜치 force-push 등 보수적 셋)을 담은 PreToolUse 입력에 대해 핸들러가 exit 2(block)를 반환하고, 회복 가능한 일반 명령은 exit 0으로 통과시킨다.",
      "verdict": "pass",
      "evidence": [
        { "kind": "command", "command": "검증자 스크립트", "summary": "rm -rf /→2, rm -rf ./build→0, git push --force main→2, git push origin main→0 재현" }
      ]
    },
    {
      "id": "ac-3",
      "statement": "비밀 파일 패턴(.env·credentials·*.pem·id_rsa·.ssh/) 읽기/쓰기 입력은 exit 2(block)로, 그 외 일반 파일 접근은 exit 0으로 처리한다.",
      "verdict": "pass",
      "evidence": [
        { "kind": "command", "command": "검증자 스크립트", "summary": "Read .env→2, Write server.pem→2, Write src/x.ts→0; read+write 둘 다 차단(pre-tool-use.ts:26-33)" }
      ]
    },
    {
      "id": "ac-4",
      "statement": "repo root 밖·시스템 경로 쓰기(scope-out) 입력은 exit 2(block)로, repo 내부 경로 쓰기는 exit 0으로 처리한다.",
      "verdict": "pass",
      "evidence": [
        { "kind": "command", "command": "검증자 스크립트", "summary": "Write /etc/passwd→2, ../→2, repo내부→0 (isOutsideRepo path.relative 검사)" }
      ]
    },
    {
      "id": "ac-5",
      "statement": "hooks/hooks.json에 PreToolUse matcher가 등록되고 surface 카탈로그가 재생성되어 drift가 0이며, 기존 v0 훅(Stop/UserPromptSubmit 등) 회귀 0으로 전체 테스트가 통과한다.",
      "verdict": "pass",
      "evidence": [
        { "kind": "file", "path": "hooks/hooks.json", "summary": "PreToolUse 키 등록" },
        { "kind": "command", "command": "bun test + surfaces drift", "summary": "surfaces 5 hook drift 0, 전체 623 pass/0 fail, lint 0" }
      ]
    }
  ],
  "status": "done",
  "owner_profile": "workspace-write",
  "child_ids": [],
  "changed_files": [
    "src/hooks/pre-tool-use.ts",
    "hooks/pre-tool-use.ts",
    "tests/hooks/pre-tool-use.test.ts",
    "hooks/hooks.json",
    ".ditto/surfaces.json",
    "tests/core/surface-inventory.plugin.test.ts"
  ],
  "risks": [],
  "runs": [],
  "created_at": "2026-06-01T15:45:59.448Z",
  "updated_at": "2026-06-01T15:55:00.000Z",
  "closed_at": "2026-06-01T15:55:00.000Z"
}
