---
{"schema_version":"0.1.0","work_item_id":"wi_260612503","from_context":"cross-PC handoff (declared_by=main, force-tracked)","original_intent":"ditto memory 기능 개선. boxwood에서 'assets/main→main 브랜치 전환 시 memory 그래프가 코드와 어긋나도 freshness가 못 잡음'이 드러나, 그 원천인 ditto memory freshness를 고치는 것. boxwood는 증상·테스트베드일 뿐.","current_state":"증분 ① 완료·origin/main에 푸시됨. 커밋 44ba881(feat: 축2 code_drift/code_dirty 검출 + warm-start 억제, 동작적), 434f478(chore: bin/ditto 재빌드, 산출물), ef841e9(docs: ADR-0015 + glossary). ac-1~11 전부 pass(autopilot completion final_verdict=pass; memory 스위트 112 pass/0 fail; 재빌드 바이너리 런타임 ditto memory status→freshness=code_drift 확인). ②~④(델타/overlay/compaction)는 게이트.","decisions_made":["축2(코드↔SoT) drift를 freshness 1급 신호로: memoryStatus가 per-owning-repo(sub-repo·submodule 포함)로 code_drift(현재 HEAD≠저장 source_revisions[].git_commit→warm-start 억제)·code_dirty(워킹트리 porcelain 더티→라벨만, inert 방지) 검출","기존 source_revisions[].git_commit 재사용 → 신규 projection 스키마 0(built_head 등 불필요)","freshness 우선순위 code_drift>stale>code_dirty>fresh — axis-1 stale이 code_dirty에 마스킹돼 stale 주입되던 회귀 차단(dialectic-2 HIGH 수정)","query/path/explain은 라벨만(거부 안 함)+drifted_repos/drifted_sources 별 필드; 소비자(memory-graph 스킬)는 drifted_sources만 소스로 직접 검증","②~④(델타/overlay/커밋공유/compaction) 게이트: '싼 구조 델타' 전제가 코드로 반증(dialectic-1 V1 critical: 구조 IR 비용은 상류 CodeQL DB 생성, HEAD-키 캐시라 더티트리=캐시미스 13.8초~3분)"],"changed_files":["src/core/memory-project.ts","src/core/memory-scan.ts","src/core/memory-query.ts","src/core/memory-warmstart.ts","src/cli/commands/memory.ts","skills/memory-graph/SKILL.md","tests/core/memory-axis2-drift.test.ts","tests/core/memory-warmstart.test.ts","tests/cli/memory-cli.test.ts","reports/design/memory-freshness-consistency-design.md",".ditto/knowledge/adr/ADR-0015-memory-freshness-axis2-code-vs-sot.md","bin/ditto"],"evidence_refs":["bun test memory 5스위트 → 112 pass/0 fail","재빌드 bin/ditto 런타임: ditto memory status → freshness=code_drift, drifted_repos=['.']","전체 bun test 1807 pass/4 fail — 4건은 기존 surface-inventory drift(stash baseline 동일 재현=무관)","커밋 44ba881·434f478·ef841e9, 푸시 434f478..ef841e9 main→main"],"failed_or_unverified":["ac-4 다수 sub-repo wall-clock latency 미측정(설계 §6, ac-4 문구 밖 unknown)","전체 스위트 4 fail = 기존 surface-inventory drift(.ditto/local/surfaces.json), 메모리 무관·범위 밖","memory status 명령 description이 아직 'fresh/stale/absent'만 언급(code_drift/code_dirty 미반영, cosmetic)"],"open_threads":["②~④ 재개 선행조건 4건(ADR-0015 D4): ①더티트리 구조 델타 실비용 측정 ②싼-델타 비가정 재설계 ③overlay 엣지 무결성 계약(dangling/모순) ④store 계약(committed 델타 tier② 추적·ADR-0013 D2 개정)","whole-projection warm-start 억제가 멀티레포에서 과잉으로 실측되면 node-level granularity(②+)","ac-4 latency가 read 핫패스 문제로 실측되면 캐시"],"next_first_check":"git pull 후 git log --oneline -4로 ef841e9·434f478·44ba881 확인 → .ditto/knowledge/adr/ADR-0015 D4 + reports/design/memory-freshness-consistency-design.md §6/D-D 읽기 → ②~④는 선행조건 4건부터(특히 ① 더티트리 델타 실비용 측정). 주의: .ditto/local의 intent.json·completion.json·dialectic 리뷰는 gitignored라 이 PC에 없음 — 결정은 ADR-0015·설계문서·이 핸드오프에 있음.","forbidden_scope_creep":["증분 머지 거부→full 재추출 결정 재논쟁 금지","[A]안정청킹·[C]IR upsert 구현 금지","②~④를 선행조건 4건 미충족 상태로 착수 금지","ADR-0013 D2(SoT git-tracked) 임의 개정 금지(별도 ADR 거쳐야)","boxwood 저장소 코드 수정·풀런 금지"],"artifact_available":true,"created_at":"2026-06-12T19:05:00.000Z"}
---

# Handoff: wi_260612503 — memory freshness 축2 검출 (다른 PC 이어받기)

## 완료 (origin/main에 푸시됨)
**증분 ① — 축2(코드↔SoT) drift 검출.** memory가 분석 시점 스냅샷이라 코드가 그 뒤 바뀌어도 freshness가 못 잡고 `fresh`로 거짓 보고하던 사각지대를 닫았다.

커밋: `44ba881`(feat 동작적) · `434f478`(chore 산출물 bin/ditto) · `ef841e9`(docs ADR-0015). ac-1~11 전부 검증.

## 구현 위치
- `src/core/memory-project.ts` — `detectAxis2` + freshness 우선순위(code_drift>stale>code_dirty>fresh). 핵심.
- `src/core/memory-warmstart.ts:~187` — 가드 분리(code_drift 억제, code_dirty 주입).
- `src/core/memory-query.ts`·`src/cli/commands/memory.ts` — 라벨+drifted 필드, scan/build `--require-clean`.
- `skills/memory-graph/SKILL.md:24` — 소비자 지침.

## 다음 (②~④ 게이트 — ADR-0015 D4)
델타/overlay/커밋공유/compaction. **선행조건 4건이 다 풀려야 착수**:
1. 더티트리 구조 델타 **실비용 측정** (CodeQL DB가 HEAD-키라 더티트리=캐시미스 13.8초~3분 — 이게 핵심 반증)
2. 싼-델타 비가정 재설계
3. overlay 엣지 무결성 계약(dangling/모순)
4. store 계약(committed 델타 tier② 추적 → ADR-0013 D2 개정)

## 첫 확인
`git pull` → `git log --oneline -4`(ef841e9 확인) → ADR-0015 D4 + 설계문서 §6/D-D 읽기 → ② 선행조건 ①(델타 실비용)부터.

**주의**: `.ditto/local`(intent.json·completion.json·dialectic 리뷰)은 gitignored라 다른 PC엔 없다. 결정은 **ADR-0015 + 설계문서 + 이 핸드오프**에 담겨 있다.

## 금지 범위
증분 머지 재논쟁 / [A][C] 구현 / 선행조건 미충족 ②~④ 착수 / ADR-0013 D2 임의 개정 / boxwood 코드 수정.
