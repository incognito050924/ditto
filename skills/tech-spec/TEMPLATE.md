# <이름> — 테크스펙

> **소비자**: DITTO(design → implement → verify) + 사람(증분 리뷰).
> **소스**: <요청·논의·문서 출처>
> **수명**: §3·§5·§6·§7·§10 = 장수명(빌드 후 ADR 승격) · §8·§11 = 단수명(빌드 후 폐기).
> **모드**: <stepwise|oneshot> · **리뷰 커버리지**: finalize 산출물에 섹션별 reviewed/skipped 기록.

---

## 1. 기능

- **이름**: <기능 이름. 관리번호가 있으면 함께>
- **층위**: <어느 표면/시스템에 속하는가>

## 2. 요약

<누가 / 무엇을 / 왜 — 한 문단.>

## 3. 배경 [장]

<왜 필요한가, 어떤 문제인가, 이전 시도는 무엇이었나. 코드베이스·프로젝트 사실은 근거(memory projection, ACG 산출물, 파일:라인)와 함께 쓴다.>

## 4. 목표

<달성할 결과. 포부형 허용 — 검증은 §6이 담당한다.>

## 5. 비목표 (변경 경계) [장]

<의도적으로 안 하는 것. DITTO의 scope creep을 막는 경계선이므로 "안 한다"를 명시적으로 쓴다.>

## 6. 완료 조건 (Acceptance Criteria)

> 관찰 가능한 술어 + evidence 종류(`test|diff|doc|browser|log`). 목표(포부)의 복붙 금지 — AC는 목표 충족을 증명하는 관문이다.

| id | 완료 조건 (관찰가능 술어) | evidence |
|---|---|---|
| ac-1 | <외부에서 참/거짓을 측정할 수 있는 술어> | <종류> |

## 7. 위험

> 위험을 적고, 각 위험을 AC 승격(§6) / 비목표(§5) / unknown(여기 잔류) 중 하나로 처리한다. 비가역(데이터 손실·스키마 마이그레이션·공개 API)이면 플래그한다. (위험을 끌어내는 질문은 작성 루프의 내부 도구 — 여기엔 질문이 아니라 결론만 남긴다.)

| 위험 | 처리 | 플래그 |
|---|---|---|
| <위험> | <처리> | <irreversible=true/false 등, 없으면 —> |

## 8. 계획 (Plan) [단]

> ⚠ **비구속(non-binding) 설계 힌트.** 구현 시 참고만 하며 더 나은 설계로 대체할 수 있다. 권위 있는 계약이 아니다. 데이터·API는 모양을 암시하는 수준까지만 — 정밀 스키마는 설계 단계의 산출물이다.

<설계 힌트. 미결정 사항은 "미정"으로 명시한다.>

## 9. 영향도 · 의존성

<이 변경이 건드리는 다른 항목 (non_local 신호).>

## 10. 기각된 대안 [장]

<검토 후 버린 선택지 + 이유. 재논의 차단이 목적이므로 근거를 함께 남긴다.>

## 11. 마일스톤 [단]

<순서 힌트. 추정이 없으면 "미정".>

## 12. 인터뷰 기록

<이 문서를 형성한 유도(인터뷰) 과정을 **요약**해 남긴다 — 원문 전사가 아니라 "어떤 좋은 질문/관점이 무엇을 드러냈고, 어떤 결정에 이르렀나". 목적은 독자가 이 문서가 *어떻게* 작성됐는지 이해하게 하는 것. 비대해지지 않게 요약 + 원본 링크. 정식 deep-interview 호출이 없어도 작성 루프의 좋은 질문·비판으로 형성됐으면 채운다. 정말 아무 유도도 없었으면 "없음".>

- 작성 과정 요약: <핵심 질문 → 드러난 맹점·새로 본 관점 → 내려진 결정. 스펙 본문엔 결론만, 그 결론에 이른 과정은 여기에.>
- deep-interview (있었던 경우만): <진입 사유 = 어떤 의도 수준 모호성이 진입 조건을 충족 / 결과 = 좁혀진 의도·범위 변화·hypothesis 가정>
- 원본: `.ditto/local/work-items/<wi>/interview-state.json` (deep-interview 시) · 그 밖의 작성 추적은 work item 트레일.

## 13. 빌드 후 처리

- **ADR 승격(잔존)**: §3·§5·§6·§7·§10 → <승격할 결정 한 줄> (`ditto:knowledge-update`).
- **폐기**: §8 계획·§11 마일스톤 → 코드가 SoT가 되면 archived.
- **링크**: <work item id> ↔ 이 문서 ↔ 생성 ADR 상호 링크.

---

## 미해결 질문

<결정 시점을 박아서 남긴다. 없으면 "없음".>
