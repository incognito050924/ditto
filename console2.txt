All work is complete and verified. Here is the summary.

What I implemented (node tzs-cli, wi_260708tzs)

All changes confined to my file scope: src/cli/commands/prism.ts and tests/cli/prism-cli.test.ts. I did not touch engine/opponent/loop/schemas (those upstream-node changes were already in the tree).

ac-4 — ditto prism tree --wi <wi> (new, pure query). Renders the issue-map tree (label · severity · state · close_reason · residual · argumentation eval · opponent status) plus the question-round timestamps. Reads only (getMap + readValueRounds) — no writeMap/appendDecision. A not-yet-seeded map is a clean message, not a crash. Verified pure by hashing issue-map.json before/after (unchanged).

ac-1 — close gate inputs wired. Added --justifying-reason and --refutation-attempted; they are upserted onto the node's evaluation annotation before closePrismNode reads them. A rejected close surfaces the reason. OBJ-4: on an A2 rejection the CLI now persists result.prism (the node stamped unevaluated) to the Run-tier issue-map.json — a durable, measurable under-think trace instead of a silent exit. The committed-base decisions tier is never written on rejection (Run-tier only, wi_260708cdl respected).

ac-3 — status re-anchor. status now loads source_request from intent.json and passes it to resolveLaunchNotification; when the one-shot launch fires it renders the reAnchor surface (original intent verbatim + achieve-vs-characterize prompt), non-blocking, console/json only. This fixes the known failing test (:153) — that test now closes via user_owned+residual to reach termination and asserts the re-anchor.

ac-5/ac-6 — production caller for the opponent seam (死배선 방지). New ditto prism opponent --wi <wi> --concern critique|dissent genuinely invokes runOpponentCritiqueRound/runOpponentDissentRound with a real OpponentSeamConfig. In the bare CLI there is no host delegate (isAvailable→false, delegate→null), so opponent-router resolves no host and the seam degrades, stamping opponent_status='host_absent' on the node and surfacing a clear degraded message. Verified the stamp is durably persisted (critique → flagged critical node; dissent → anchor/root).

Evidence (fresh)

- bun test tests/cli/prism-cli.test.ts → 17 pass / 0 fail, 80 expect() calls, exit 0 (was 1 pre-change: the :153 launch test).
- Full suite bun test → 4184 pass / 11 skip / 0 fail across 338 files, exit 0. (The "not a git repository" line is a benign temp-dir message from an unrelated test.)
- bun run lint (biome, the commit gate) → exit 0, no fixes.
- bun run build:bin → OK; working-tree bundle smoke: ./bin/ditto prism tree --wi wi_finalcheck1 renders the tree, exit 0; prism tree on an unseeded WI prints "아직 이슈맵이 없어요", exit 0.
- Manual smokes confirmed: close-without-flags → A2 reject + persisted evaluation:"unevaluated"; close-with-flags → rejection reason drops justifying_reason/refutation_attempted and lists only grounding (proves the flags reach the gate); prism opponent --concern critique/dissent → host_available:false + host_absent stamp.

changed_files (repo-relative)

- src/cli/commands/prism.ts (+317)
- tests/cli/prism-cli.test.ts (+212)
- bin/ditto (rebuilt dogfood bundle — reflects working tree)

git diff --stat for my two source files: 520 insertions(+), 9 deletions(-).

Notes / risks

- Not committed (per instructions).
- Do not revert the other working-tree changes: src/core/prism/{engine,loop,opponent}.ts, src/schemas/prism.ts, schemas/prism-issue-map.schema.json, tests/core/prism-{engine,opponent}.test.ts are the upstream nodes' deliverables (already done). .ditto/work-items/wi_260707phi/ (record.json + one event) is a sibling node's concurrent work-item promotion (event ts 06:41:02, actor workspace-write) — it pre-dates my suite run and no test references it, so I left it untouched to avoid a parallel-WI clobber. console.txt is an external harness capture of my session narration, not my creation.
- No test-isolation leak: my tests use wi_prismcli01 in mkdtemp temp repos; find .ditto -path '*wi_prismcli01*' is empty.
- Limitation (honest): a fully-green critical resolved close (exit 0) additionally needs structural grounding (a child decomposition or a real opponent artifact), which the bare CLI can't synthesize with no host and no seed --parent. So ac-1's happy path is proven via the flags-consumed rejection-reason change rather than a green close; the launch/termination test reaches termination through the MODEL-1 user_owned+residual path instead.